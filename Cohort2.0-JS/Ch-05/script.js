Operators in JavaScript

// arithmetic operators -> mathematics related

// assignment operators -> vakue assign karne ke liye


a += 3;
a = 22;

// comparison operators 
// == -> sirf value check karta hai 
// === -> strict equals , checks the data types as well 

// !== -> use this instead of != .

// logical operators  -> && , || , ! .

// Ternary Operator 

//  condition ? doThis(true) : doThat(false) 

// Type Checking Operators 

// typeof , instanceof .

// String Operators :
// + (for concatenation)

// Nullish Coalescing Operator : 
// (fallback only when null / undefined )

eg : user ?? console.log(); // means that if user is null then ?? ke aage wala run kar jayega 

// optional chaining

// variable hoisting in js :

// variable ko use kara jaa skta hai banane se pehle

console.log(b);
var b = 12 ;

// var b ;
// b = 12 ;

// declaration top of the file jata hai and initialisation wahi rehta hai jaha variable tha !
// hoisting doesnt work with let . 

// temporal dead zone : variable ke banne wali line se pehle ki jagah .





